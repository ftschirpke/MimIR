[pytest]
testpaths = .
python_files = test_*.py
norecursedirs = venv stubs __pycache__ .git build
addopts = -ra --strict-markers
markers =
    needs_clang: requires clang on PATH (regex/codegen end-to-end tests)
    slow: end-to-end compile + execute tests
